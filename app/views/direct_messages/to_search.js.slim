| var result = $('#result');
| result.find("li").remove();
- @users.each do |user|
  | result.append('<li data-user-id="#{user.id}">#{gravatar_for(user,size:40)} #{user.name} @#{user.unique_name} </li>');
